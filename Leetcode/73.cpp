#include <vector>
#include <iostream>

class Solution {
public:

    void setZeroes(std::vector<std::vector<int>>& matrix) {
        
        for(size_t i = 0; i < matrix.size(); ++i){
            
            std::vector<int> row_zeros, column_zeros;

            for(size_t j = 0; j < matrix[i].size(); ++j){


            }
        }
    }
};